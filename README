# bluetooth keyboard auto loader
Script to auto pair a bluetooth keyboard to current device and hook it to the usb output

## Requirements
* bluetoothctl

## Systemd
Copy bluetooth-keyboard-auto-loader.service to /etc/systemd/system